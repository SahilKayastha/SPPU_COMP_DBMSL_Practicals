Q.6) SQL Quries
1.	Create a table emp with following fields and constraints 
Eno –(Constraint:- primary key and apply sequence starts with 101) ,
Ename –(Constraint :- not null) 
Address ––(Constraint :-default ‘Nashik’) ,
Joindate, 
2.	After table creation add field - Post in the emp table. 
3.	Insert some data in emp table.
4.	Create Index on Ename field of employee table. 
5.	Create View on employee table to show only Ename and Salary.


Create the emp Table:
The column Post will not be added but will be included later.
CREATE TABLE emp (
    Eno NUMBER PRIMARY KEY AUTO_INCREMENT,  -- Primary key constraint
    Ename VARCHAR2(100) NOT NULL,  -- Ename cannot be NULL
    Address VARCHAR2(100) DEFAULT 'Nashik',  -- Default value constraint
    Joindate DATE
);

-- Create a sequence for the Eno field starting with 101
ALTER TABLE emp AUTO_INCREMENT = 101;

-- Add the Post field to the table after creation
ALTER TABLE emp
ADD Post VARCHAR2(50);

-- Inserting data into the `emp` table without specifying `Eno` (as it's auto-incremented)
INSERT INTO emp (Ename, Address, Joindate, Post)
VALUES ('John Doe', 'Pune', '2023-01-10', 'Manager');

INSERT INTO emp (Ename, Address, Joindate, Post)
VALUES ('Jane Smith', DEFAULT, '2023-03-15', 'Developer');

INSERT INTO emp (Ename, Address, Joindate, Post)
VALUES ('Michael Johnson', 'Mumbai', '2023-05-20', 'Analyst');

INSERT INTO emp (Ename, Address, Joindate, Post)
VALUES ('Linda White', 'Nashik', '2022-12-01', 'HR');

INSERT INTO emp (Ename, Address, Joindate, Post)
VALUES ('Robert Brown', 'Delhi', '2023-07-18', 'Sales');

INSERT INTO emp (Ename, Address, Joindate, Post)
VALUES ('Emily Davis', 'Nashik', '2023-02-05', 'Designer');


Create Index on Ename:
Creating an index on the Ename field helps in faster retrieval of data when searching or filtering by employee name.
CREATE INDEX idx_ename ON emp(Ename);

SHOW INDEX FROM emp;
Create a View on the emp Table:
The view will display only the Ename and Salary. Assuming Salary is a hypothetical field that might exist in the extended table, it can be included. If the Salary column doesn’t exist yet, you can skip it in the view or modify the structure later.
-- Assuming Salary field exists
CREATE VIEW emp_view AS SELECT Ename, 50000 AS Salary FROM emp;
SELECT * FROM emp_view;
-- Static salary for simplicity; otherwise, include the actual salary field

